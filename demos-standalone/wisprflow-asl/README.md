## WisprFlow ASL

ASL hand-gesture recognition demo built for WisprFlow. Uses TensorFlow.js hand-pose detection to recognize ASL signs in real time — runs entirely in the browser with no server calls.
